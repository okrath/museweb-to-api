{
  "name": "museweb-to-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local gateway that exposes a signed-in muse.ai web session as OpenAI- and Anthropic-compatible HTTP APIs.",
  "bin": {
    "museweb-to-api": "./dist/index.js"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts serve",
    "start": "node dist/index.js serve",
    "muse:login": "tsx src/index.ts login",
    "muse:probe": "tsx src/index.ts probe",
    "build": "rimraf dist && tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "browser:install": "playwright install chromium"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.22.0",
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "dotenv": "^16.6.1",
    "fastify": "^5.6.0",
    "nanoid": "^5.1.5",
    "pino": "^9.9.5",
    "pino-pretty": "^13.1.1",
    "playwright": "^1.58.0",
    "turndown": "^7.2.0",
    "turndown-plugin-gfm": "^1.0.2",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "@types/turndown": "^5.0.5",
    "rimraf": "^6.0.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
